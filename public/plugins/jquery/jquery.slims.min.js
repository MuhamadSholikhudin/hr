// function Resign(){var resignnik = document.getElementById("resignnik").value;var resignktp = document.getElementById("resignktp").value;$.ajaxSetup({headers: {'X-CSRF-TOKEN': jQuery('meta[name="csrf-token"]').attr('content')}});$.ajax({type: "POST",url: 'http://10.10.42.6:8080/pages/apigetemployee',dataType: "json",data : {number_of_employees: resignnik,national_id: resignktp},success (status){if(status == 200){var urlLocation = "http://10.10.40.190/infohrd/resign.html";swal({title: "Apakah anda yakin ingin resign ?",text: "Jika anda yakin ingin mengajukan resign tekan OK dan jika anda tidak jadi tekan CANCEL !",icon: "warning",buttons: true,dangerMode: true,}).then((willDelete) => {if (willDelete) {                   window.location.assign(urlLocation);} else {swal("Permohonan resign tidak jadi !");}});}else{swal("Data Karyawan tidak di temukan !");}}});}   

// In your Javascript (external .js resource or <script> tag)
var url = "http://127.0.0.1:8000/";

function Resign(){
    var resignnik = document.getElementById("resignnik").value;
    var resignktp = document.getElementById("resignktp").value;
    $.ajaxSetup({
        headers: {
            'X-CSRF-TOKEN': jQuery('meta[name="csrf-token"]').attr('content')
        }
    });
    $.ajax({
        type: "POST",
        url: url+'pages/apigetemployee',
        dataType: "json",
        data : {
            number_of_employees: resignnik,
            national_id: resignktp
        },
        success (status){
            if(status == 200){
                var urlLocation = url+'pages/resign';
                swal({
                    title: "Apakah anda yakin ingin resign ?",
                    text: "Jika anda yakin ingin mengajukan resign tekan OK dan jika anda tidak jadi tekan CANCEL !",
                    icon: "warning",
                    buttons: true,
                    dangerMode: true,
                })
                .then((willDelete) => {
                    if (willDelete) {                   
                    window.location.assign(urlLocation);
                    } else {
                    swal("Permohonan resign tidak jadi !");
                    }
                });
                
            }else{
                swal("Data Karyawan tidak di temukan !");
            }
        },error(){
            swal("ERROR!");
            
        }
    });
}

// Use Javascript
function today(){
    var today = new Date();
    var dd = today.getDate();
    var mm = today.getMonth() + 1; //January is 0 so need to add 1 to make it 1!
    var yyyy = today.getFullYear();
    if (dd < 10) {
        dd = '0' + dd
    }   
    if (mm < 10) {
        mm = '0' + mm
    }
    today = yyyy + '-' + mm + '-' + dd;
    return today;
}

function ResignCheck(){
    var resignnik = document.getElementById("resignnik").value;
    var resignktp = document.getElementById("resignktp").value;
    $.ajaxSetup({
        headers: {
            'X-CSRF-TOKEN': jQuery('meta[name="csrf-token"]').attr('content')
        }
    });
    $.ajax({
        type: "POST",
        url: url+'pages/apigetresign',
        dataType: "json",
        data : {
            number_of_employees: resignnik,
            national_id: resignktp
        },
        beforeSend: function() {
            $("#spinner").show();
         },
        success (status){
            if(status[0] == 200){
                $( "#checkdisplay" ).show("slow");   
                $("#checkname").show("slow");   
                document.getElementById("status_employee").value = status[1];
                document.getElementById("dateresign").value = status[2];
                document.getElementById("name").value = status[3];
                document.getElementById("dateresign").setAttribute("min", status[2]);
                swal("Data Karyawan di temukan ! "+ status[4]);
            }else if(status[0] == 202){
                $("#checkdisplay").show("slow");   
                $("#checkname").show("slow");   
                document.getElementById("status_employee").value = status[1];
                document.getElementById("dateresign").value = status[2];
                document.getElementById("dateresign").setAttribute("min", status[2]);
                document.getElementById("name").value = status[3];
                document.getElementById("dateresign").disabled = true;
                swal("Data Karyawan di temukan ! " + status[4]);
            }else{
                $("#checkdisplay").hide("slow");   
                $("#checkname").hide("slow");   
                document.getElementById("status_employee").value = "";
                swal("Data Karyawan tidak di temukan ! " , { className: "red-bg"});
            }
            $("#spinner").hide();
        },error(){
            $("#spinner").hide();
            swal("ERROR!");
        }
    });
}



